---
version: 1
slug: "apps-web-src-features-reports"
primary_target: "apps/web/src/features/reports"
related_targets: ["packages/api/src/routers/agency-ops/reports"]
---

Visitor mode: Operate. Scope: Agency Reports compose studio (`/agency/reports` and `/agency/reports/$reportId`).

Audience: owners/managers (sometimes members) hunting every grouped cluster on this period’s export document, excluding waste/bad rows, then exporting. Tracker remains the Clockify-adjacent log editor.

Action: edit first-class Scope / Shape / Recipes / Export; proof is the full grouped paper. A quiet client outline (name + `nH:nM:nS`) jumps to chapters; virtualization paints the document. Inspect or fix a cluster only through the existing Tracker-style details dialog. Saved reports are recipes (filters + exclusions), not frozen row snapshots; `$reportId` hydrates the same studio.

Constraints: incumbent Agency DESIGN.md / shadcn quiet instrument; no command-bar stacked above a dumped table; no nested cards or liquid glass; no BI charts; no 5k ungrouped table in the view; the paper is the complete grouped document (every scoped client chapter and aggregated row) with outline + virtualization carrying load, not 3-client / ~8-row caps; hour totals from SQL summary; waste/grouping/amount/`nH:nM:nS` product rules stay; Excel styling/grouping/per-client files untouched.

Direction: options are the work; the paper is the full document, not a teaser of Excel. Desktop two panes (sticky options, trailing document with outline). Mobile stacks options then a compact jump list above the paper. Focal moment: click a client in the outline, land on that chapter, exclude a cluster, keep going.
