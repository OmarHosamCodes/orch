/**
 * Marketing launch gate — keep below-the-fold sections in the tree but hidden until ready.
 * Set to `true` when pricing and the rest of the landing ship.
 */
export const LANDING_BELOW_HERO_VISIBLE = false;
