## Learned User Preferences

- Agency work surface / time tracking should match Clockify UI and UX closely (including action parity); avoid inventing custom flows that diverge from Clockify unless explicitly requested; waste (and similar) marks must apply only to the targeted entry, never auto-select or mark a whole day/group when marking one; waste totals and classification everywhere automatically include waste-flagged tasks, waste-named projects, and the entry markAsWaste boolean (not the entry flag alone); Tracker waste is a dismissible tag (X to unmark); Tracker’s live log uses a dedicated Waste column before time (waste/mark states) with unmark on entry logs—including bulk unwaste on grouped rows; Reports is a compose studio (expanded options + limited export-document preview), not a Clockify live-log dump—waste still appears on the paper before duration, mixed grouped rows use a quiet partial-waste chip, and unmark/time/delete/rename happen in the Tracker-style details dialog (mutations persist; no inline preview editing); collapsed xN/grouped rows that mix waste and non-waste use a quiet partial-waste chip (not a full waste tag on the whole group), and dismiss/unmark only clears dismissible entry-flagged waste—plus a quiet rectangular waste treatment (not wavy strike, not spanning project/actions); Tracker should include an industry-standard collapsible My Tasks right rail (prefer shadcn): both list-manage and play-to-track on existing project tasks (not a separate personal-todo model); filter pills not tabs; docked sibling on desktop with Sheet on small screens; rail styling and top edge align with the tracker (no offset); group by client with a quiet per-task meta row (assigner avatar, self as exact `me` and others avatar-only, plus tracked-vs-estimate time with error styling when over estimate, and project name); My Tasks titles open/toggle a cover-slide task thread over tracker + entries (rail stays; title click is thread-only, not select/play)—chat-first with a visible compact task header (not buried under tracker), industry-standard max-width column, and Orch-in-thread via composer badge (not the global Orch dock as the chat surface); project/client pickers reuse the tracker task-chooser pattern; create-form task chooser sits on its own line with remaining controls on a second line; Add is a full-width `Add` button (no dropdown arrow) using the primary theme color, becoming `Update` when the selected task already exists; owners/assignees use a compact max-width avatar stack that only shows avatars that fit with smart +N overflow (plus affordance when solo); optional task estimation via a clock-icon control; morphing transitions on task state enter/leave, while rail collapse/expand follows the width uncollapse animation (not a separate morphing badge or expanding collapsed column).
- Prefer choosing an existing task over creating a new one in task selectors and suggestions; do not use dedicated clear-X or switch/replace icon buttons on the tracker task chooser or time-entry rows—change tasks by opening the task chooser itself; the unified agency task chooser should parse nested multi-token queries (client/project/task), smart-collapse/expand and highlight by match location, keep a matched client/project’s tasks available for manual expand (not empty `tasks: []`), and prefetch/hydrate the catalog before the dropdown opens (no “Load more tasks” gate).
- Timer and time-entry state must persist on the server (DB or Redis), not browser or device storage, must survive refresh, and must stay editable mid-tracking with changes durably saved.
- Optimistic UI must not flicker or diverge from server/cache truth; for critical task/timer CRUD, prefer DB/cache-confirmed updates over local-only optimistic state; Agency task-chooser infinite query keys include an `infinite` segment before `catalog`/`search`—cache patches must treat any key containing `infinite` as `{ pages }` shaped, never list-shaped, or create-task optimistic patches corrupt Tracker; focused tracker/description inputs must not stutter or lose edits from live timer or cache sync while typing; timer start/stop must queue (no silent drops); pending/saving and stop blockers must be visible and accessible; picking a description suggestion must flush so mid-track description sync does not stick.
- Workspace agent (Orch) should be available above all pages and unified across Agency and Canvas (Ask / Plan / Agent on both — do not coerce Canvas Plan to Ask); writes never apply directly on either surface — propose or confirm-plan into pending proposals with before/after `ui_present` illustration, then human Approve/Reject (Canvas uses the same proposal model, with real workspace-block / `workspaceNode` preview rather than generic schema/react stand-ins); tool catalog follows the route (`/agency*` vs Canvas) and unlocks the other surface via scope/intent tags (not always-both catalogs); keep tool calls lean/token-efficient and prefer `ui_present` canvases when useful (avoid noisy tool spam with no artifact); artifact canvases must be openable from the creating message, and after Canvas approve a secondary open-on-board path (`/node/:id` or `/canvas`); the latest question, plan-approve, and canvas should stay sticky at the chat end (Cursor-like) with deliberate delight animation; composer history and sniper mode use a floating bill; scope/design selection uses app-suitable terminology and is not click-only; collapsed composer expands on hover and collapses on click-outside, with morphing expand/collapse from the bill (no circular reveal; expanded dock must keep an explicit card border-radius during morph—never inherit the collapsed pill radius; collapsed pill sits lower and wider; expanded composer stays bottom-docked); when an Agency task thread is open, hide the global Orch dock and morph dock ↔ thread Orch badge (premium smooth morph); an active working thread shows a collapsed shimmer (hover: “Working…”); expanded chat is assistant-ui `Thread` as a quiet scroller inside the existing Orch card (collapsed pill stays the only global entry/composer — same runtime, not a second stack); website-capable `@assistant-ui` registry items must be wired into that live composer/thread, not installed unused; do not mount assistant-ui demo/showcase chrome (quota banners, voice/MCP/runner disconnected panels, extras strips, Thread follow-up footers, paper demo cards); composer mic dictation sits on the right (product dictate, not demo voice chrome); non-Agent modes and active scope appear as icon+removable dismissible tags that clear back to Agent; model picking uses Fast/Balanced/Pro tiers with Auto and Free toggles, shown as a single-word label with tooltip details (not a flat model list alone); composer toolbar controls have tooltips; attach merges into the plus menu; composer supports paste/upload attachments (images plus txt/md/json).
- When verifying agency UX, use the browser; for Clockify-parity work, compare against Clockify in-browser until the goal is met; for major UI redesigns or surface visual direction, run Impeccable `shape` with Design Inspiration and multiple complete directions in Open Design first (real UX/UI/animation—not showcase-only; each direction must expose 100% of current surface features and actions, not partial showcase comps), then confirm before implementing.
- Default git branch for rebase, merge, deploy, and fix PRs is `dev`; when explicitly asked for commits on multi-step work, prefer small micro-commits per logical update.
- Primary app navigation is a left sidebar rail shared by Canvas and Agency: team controls at the top (not a product logo; each team has one agency/team logo for profile), distinct sidebar background from the main content, a featured actionable Needs-action notification card in the rail footer (product Updates are always the highest-priority card—not buried in settings; dismiss/act advances to the next; overflow via a quiet “N more”; every notification actionable—not a passive bell-only inbox; app updates are not in Settings—they always take this featured card at highest priority, above Needs-action), and a Sign out button at the bottom (not a separate page); reference-style profile card whose avatar opens account actions/preferences (including notifications), and the sidebar collapse button in the top bar; no outer padding around sidebar/top bar, no expand-on-hover, and Agency section tabs should remain reachable via hover menu when the rail is collapsed; sidebar and top bar must read as one connected shell surface; the Agency top-bar segment crumb opens a segment switcher menu (not a static label only); nested Agency rail routes use a quiet thread line (spine + joins; hidden when collapsed); Management secondary panes drill into the full primary rail (including the Canvas tab) as a nested list styled like the sidebar with a thread line for nested routes, with a recognizable Back control that works in compact/collapsed mode and only swaps rail content (stay on the current Management page; leave Management via the segment switcher); member avatars should match the shared AgencyMemberAvatar treatment used elsewhere.
- Styling should stay on the standard shadcn theme tokens (light and dark); prefer base theme tokens over custom ones; use shadcn primitives (e.g. Checkbox, Select, Collapsible, Label, Calendar + Popover date picker) instead of custom/native or Block* inputs—especially on Agency settings forms and Canvas node-block editors; Canvas node-page editors stay distilled (no nested cards or hero-metric strips; motion only at empty/complete/error/wait); leave/date-range calendars should stay open until explicit confirm; on member profile prefer “Off days” terminology over Leave, and contribution-day click should offer select-range or add-off-day using that same calendar pattern, with Remove off day (confirm dialog) for mistaken entries from the calendar day menu; member profile panels/plates/strips/calendar/activity rows use neutral shadcn card surfaces with semantic color only on SVG glyphs, charts, and micro-indicators (not tinted plate/card/row backgrounds; calendar present/off use neutral cells + bottom bar/dot); the stats-grid instrument plates (Off days / Period hours / Present / Waste) must not be reverted to ring stat cards; Money period stats (`MoneyStatsSection`) use the same instrument-plate language (D01): neutral surfaces, fixed-size SVG geometry glyphs (box size does not change—only colors) that tint by information flow (income/allowance constructive; ROI/profit/loss more destructive) with subtle delight animations, hierarchy-dominant over mono metrics and labels, all metrics visible without Details collapsibles; Expenses live inside Bills as a filter next to Adjustments (no standalone Expenses section); Bills Clients/Team/Adjustments/Expenses use shadcn tables with an actions column (settle + details opening a detail sheet with per-line actions and adjustment control; expense create/edit/payment stay dialogs), client Bills rows show hours as `nH:nM:nS` and amount in the client's rate currency, All preselected among party filters (omit redundant This period—the page period chooser already scopes), mono tabular numeric cells, and remaining as warning only when > 0; Bills create is a dropdown to choose what to add; expense create/edit/payment dialogs use shadcn primitives throughout; Bills use unified panel header chrome (`money-panel-chrome`: title row, metric block, filter pills) with search via `AgencySearchHighlight`; Money surface omits the standalone Period payout run block; Money dialogs should reuse the Dashboard command-bar client chooser (single-select) and the member-profile off-day time-range chooser; contribution heatmaps (member profile and Agency Resourcing team presence) keep fixed square size and fill empty days as zero/empty cells (do not shrink the grid); Resourcing presence is one team heatmap for who is working vs absent/out—not separate workload vs working tabs; do not add custom styling to shadcn UI components unless needed; when applying an external theme pack, keep the product fonts; do not use liquid glass (`ui-liquid-glass-*` / canvas ui-chrome-glass) for overlays or Canvas chrome—replace with standard shadcn surfaces; Tracker chrome should use default card borders (avoid colorful status borders); tracker loading uses a whole-tracker shimmer (not text loading indicators); tracker manual-add controls use shadcn (not native) with spacing and sizing aligned to tracker rows; stop/blocker alerts sit under the tracker bar, not in the main row; tracker main row height should match time-entry rows; tracker description focus should stay subtle (no bold/heavy focus ring); Tracker/entry-row billable `$` lives in the ⋮ options (not a rail icon)—the old `$` slot is a hover/rail link icon that opens a dialog to add entry links; global toasts appear bottom-right.
- Time-entry durations must stay second-precise (`hh:mm:ss`) end-to-end with no rounding to whole minutes on save; start/end clocks may still display Clockify-style without seconds; Tracker entry duration fields should use Clockify-style HH:MM:SS segment editing (click/focus a unit and type digits to replace that segment; ↑/↓ nudges the focused hours/minutes/seconds unit, not always ±1 second).
- Tracker description suggestions must never implicitly change or remove the selected task; a suggestion applies only when the user explicitly picks it, and editing the description must not resurrect a previously cleared task; the suggestion dropdown must align exactly to the description input (same left edge and width).
- When backfilling time entries from Cursor (including Cloud), OpenCode, and Codex chat/work history (agency-time-gap-fill), use a from→to range (default today), check/report first then insert only when asked; new entries must not overlap each other or existing entries, and report before/added/new-total hours.

## Learned Workspace Facts

- This repo is the Orch/brainiac Bun monorepo; agency time tracking is a core product surface (`apps/web` task-management / agency work features); Agency member profile is a GitHub-layout-inspired HR surface (identity/personal info, off days, activity timeline) with an Alerts panel in the main column under Contribution (hours-logged week bars appear only after clicking a contribution day; Ask Orch stays in the right rail)—Alerts and the stats grid above Contribution share instrument status plates (kind-tinted SVG geometry glyphs, mono metrics, short labels; Present plate reads as attendance streak; manager note/notify/snooze/dismiss in a secondary detail dialog)—gauge plates open morph dialogs (month pace visual for Hours)—system detectors (abnormal day hours, month/quarter pace risk, waste spike) plus manager custom alerts; team can view, managers/owners add/remove/snooze and send notes (`member.alert` → `/agency/members/:userId`); alert actions should deep-link to the alerted period (`tm:` tenure-month and quarter keys) and that day’s entries with a shimmer highlight without clearing calendar month override; member-profile calendar shows daily minimum progress on any day with entries (including weekends/holidays/off days) and legend dots without counts; when tenure tracking is enabled, profile calendar/month pace/gauges/alerts use fiscal tenure months (`tm:YYYY-MM-DD` keys) with calendar-month fallback when tenure is off—the shared “This month” range preset stays calendar month; the selected profile period (including multi-month and tenure-quarter ranges) drives calendar, gauges, month-pace, attendance, and alerts—not calendar-month-only; quarter pace uses tenure quarters (e.g. 26 Jun–25 Sep), not calendar Q3; tenure policies include work schedule (required daily hours, week start, weekend duration), quarter-style `monthlyMinHours` for month-pace alerts, and `offDayReduceHours` (weekday off days reduce month minimum in hours gauge + month pace; off days/holidays included in working-day counts; attendance streak skips weekends and does not break on off days/holidays); Management > People Off days step configures allowance only (no recorded off-day entries list yet); Agency Management is Operations + Commercial hubs (Resourcing, People / Money)—Tags and Rates are not Management nav panes (member rates live under People guided config); People is `/agency/management/people` (internal pane id may stay `tenure`) as a directory-cards hub with guided member config and team defaults, and member-profile alert policy (each detector on/off plus one simple threshold—not a new Management pane); Money (renamed from Invoices) uses a team-selected agency currency (ISO 4217; soft-locked once money data exists) with product language always `amount` (integer minor units under the hood; never expose “cents”); foreign inputs store source amount/currency plus FX-resolved agency `amount` (manual team FX rates with optional live suggest; client billable rates may stay a static source-currency amount such as USD and convert to agency currency each period at the period-locked FX rate; expenses use the same optional source-currency picker in create/edit but convert once at save with current team FX then freeze—strip, Pay, and scoreboard stay agency currency); Bills are compose-on-demand—client/member groups show current plus prior-period carry lines nested as narrower sub-rows under the main balance, ephemeral branded invoice/payslip preview (not persisted), Export with carry-on multi-select (combine or split by period), and Adjust for collect/pay/partials plus discount/surcharge/debt/Extra (Extra is an Adjustments-filter section that raises period Total income and ROI, never cost; client Adjust is Collect; reversing client collection is Uncollect, which zeros received and returns remaining/sent rather than an irreversible refunded status; people/member Adjust stays Pay; paying Ready soft-exports then records payment); client discount/surcharge/debt apply immediately to the selected period’s bill (Ready or an outstanding invoice)—Bills totals/remaining and scoreboard income/ROI move on save (surcharge/debt up, discount down), not deferred until next export—and the client bill detail sheet exposes per-line actions plus full adjustment control (not only a single Adjust CTA); All-party Bills filter Paid/Partial/Outstanding; client invoices use the client billable rate only (member rates do not affect invoicing); billable rate inheritance is client → project override → task override (highest set wins; unset levels inherit; task rates are edited on the project page); Total income defaults to billable external-client amounts excluding waste, and invoicing moves amounts into received vs remaining (client bills use an external filter badge; dismissing it also shows internal clients); expenses (Bills → Expenses filter) cover subscriptions + recent with a real Pay action that records a period occurrence and advances next due (not only mark-as-paid) so paid subscriptions stay in that period’s expense/scoreboard total while leaving the Upcoming Due list, with Due/Paid visibility filters, plus subscription start dates for clearer next-payment timing; expense type (subscription vs one-time) is changeable on edit; variable subscriptions support an optional initial/first-payment amount; Adjust can add a team salary pool (manual period total; pool-level partial Pay reduces shared remaining while scoreboard uses the full pool total—not per-member settlement rows); Money cohort rules define who qualifies and chip formulas define how much (formulas may bind a ruleId; custom rules need an enabled formula with a payout section to create amounts); formulas with payout sections sync to bill lines via syncFormulaLines (auto on save and period load; disabling a formula does not auto-remove existing adjustment lines—dismiss removes that line from existence); formula number chips store minor units but display and edit in major currency units (10 = 10 EGP in the chip UI, not raw minor/cents); PBC/charity/debt/Extra bill lines use the Adjustments filter, device comp/paid vacation/team loss use People; scoreboard Team profit/ROI include synced PBC and charity in the cost base and profit/loss share is team profit ÷ 2 (Fin-Sheet team profit is the amount row—income minus salaries, expenses, debt/discount, device comp, paid vacation, charity, and PBC); Resourcing uses a team-wide presence/workload heatmap in the same contribution-heatmap pattern as member profile; Agency Projects/Clients aim for industry-standard agency UX (Clockify/AgencyPro/Productive-class) with Journey + tasks kept first-class; projects soft-delete with confirmation into a trash/archived filter (restore supported; ~30-day retention before purge; soft-deleted projects excluded from all agency listings; team-wide `projectTasks.list` likewise excludes soft-deleted and archived-client projects so the task chooser catalog does not orphan tasks).
- Local dev can refresh Postgres from a Railway production dump into Docker (`packages/db` compose reset + `pg_restore`) for realistic Money/UI verification; keep production DATABASE_URL and credentials out of commits.
- Production hosting is on Railway (Postgres; Redis is also used for server-side persistence/cache); Clockify data can be imported with `bun run db:import:clockify`; Sentry issue fix automation opens fix PRs against `dev`.
- Product features follow the golden-file layer pattern; Agency Time Tracking is the exemplar.
- Authenticated chrome is a left sidebar rail plus a thin connected top bar (breadcrumb, team switcher, notifications, collapse control) in `apps/web/src/features/app-shell/`; spatial dashboard uses `app-shell--spatial` and Agency uses `app-shell--execution` on the same topology; each team has a single agency/team logo used in team settings and the shell profile; global workspace agent composer lives under `apps/web/src/features/workspace-agent/` with expanded chat as assistant-ui `Thread` (`apps/web/src/components/assistant-ui/`, website-capable `@assistant-ui` registry items only — do not overwrite `@/ui` primitives; wire them into the live bottom-docked composer/thread, including mic dictation on the right) on a custom transport over oRPC `agent.chat.turnStream` (no parallel `/api/chat`); streamed answers plus generative UI artifacts (`ui_present`, including `workspaceBlock`/`workspaceNode` previews via the board block registry) and an openable agent canvas overlay; Agency and Canvas share one composer/runtime with route-default tools plus intent-unlock merge, and Canvas mutations use the same pending-proposal bus as Agency.
- `apps/web` uses TanStack Start (Vite + TanStack Router) with prerendered marketing/login HTML and an SSR authenticated shell; Agency pages use absolute path URLs (not `?section`/`?manage` query routing)—People is `/agency/management/people`; keep filter/protocol search params (`from`/`to`/`focus`/`alertId`/etc.); do not SSR Tracker/Reports day grids; navigation helpers live in `@/lib/navigation`; Hono/`apps/server` remains the API for auth, oRPC, uploads, and live WS (web process proxies those paths); browser/Vite code must not import the `@orch/agent` barrel (it pulls dotenv/`process` and crashes the client)—use `@orch/agent/types` or `@orch/agent/model-routing`.
- The spatial home route is `/canvas` (formerly `/dashboard`); it is a MagicBento-style bento grid (React Bits + gsap) with adaptive glass cards; Canvas chrome stays off the plane (context bar shares the canvas surface; the canvas is the product); Canvas workspaces stay user-scoped with existing team-sharable nodes (`visibility: team` / `teamId`) as the Agency bridge (no separate team-board product); north-star Agency↔Canvas links are project/task `agencyRef` on those team-shared nodes (Money/time stay Agency-only); knowledge objects are a structured store with the infinite canvas as a spatial view (not nodes-only); create knowledge from the right-click menu via specialized shadcn dialogs—one dialog per type, no in-dialog type switcher, no floating create HUD; workspace Node create (`createNode` / WorkspaceEditorModal) stays its own path and must not be swallowed by knowledge dialogs; light kind color is ok; node-page block editors (`/node/:id` and agent preview) are the polished editor surface—canvas board cards stay summaries; marketing landing uses React Bits WebThreads (with GradualBlur as a scroll cue); auth uses React Bits Scanner (bento restyle stays reverted); landing Canvas/Agency/Agent previews must mirror real product UI from the codebase, not invented mockups.
- Agency’s primary time-tracking nav segment is labeled Tracker (not Work); Dashboard and Reports hour breakdown uses a shared Paid / Waste / Internal composition; waste is entry `isWaste` OR task `isWaste` OR word-boundary “waste” in the task/project name, applied across Reports, Dashboard, Money, payouts, member profile, and Tracker display; waste hours are subtracted from displayed totals on live and created reports; on Dashboard, Project share morphs into a Sankey-style flow that splits Internal into billable vs non-billable; Agency Dashboard and member-profile time-range defaults to the current calendar month clamped to the tenure start/end window (not a quarter); Reports is a compose studio at `/agency/reports` and `/agency/reports/$reportId` (same layout): first-class Scope / Shape / Recipes / Export, a limited export-document preview (not the full log; hour totals from SQL, sample rows only), and Excel combined or per-client from this page; saved reports are recipes (filters + exclusions), not frozen row snapshots; Tracker keeps Clockify live-log parity; Reports preview waste stays before duration with a quiet partial-waste chip on mixed xN rows; entry-detail mutations (unmark waste, time edit, delete, rename) persist after refresh via the Tracker-style details dialog—no optimistic false positives and no inline preview editing; Shape options (waste sources, field visibility, merge same task names) are first-class in the studio, not nested ⋮; preview and export use `nH:nM:nS` totals and amount in the client’s rate currency on client banners when a rate exists; Export can write per client (one file per client); Excel exports should be human-readable and styled (large text, color, larger project names with spacing between projects, report details/metadata hidden from the sheet).
- Reports list similar task titles consecutively under each project with two-tone row striping so clusters scan as blocks; aggregated Create Report / report rows join all non-empty descriptions with separators (never blank when descriptions exist) and likewise join multiple assignees and time-entry links with separators (no mixed count); Tracker and Reports persist entry/timer URLs via a Link column (hover/rail icon opens the add-link dialog); aggregated-row entry details reuse the Tracker grouped time-entry log UI, including bulk actions and bulk unwaste when multiple entries are present (mixed-waste xN/grouped rows show a partial-waste chip, same as Tracker); member-profile activity should likewise merge similar events with a multiplier and details dialog and colorize like Tracker entry rows; task names use the full task-cell width; task/description text (Arabic or English) aligns to the start while inputs follow text direction; a dim partial-height vertical separator sits between task and description cells.
- A weekly Cursor automation produces a canvas report of Cursor/OpenCode/Codex-chat vs tracked-time gaps and the proposed additions/new totals before entries are inserted.
- Agency task thread messaging: any team member can read; only current task assignees can send text/attachments (a non-assignee creator is read-only); optimistic send + live sync via the new `@/ui/message` / `@/ui/message-scroller` stack—do not restore the pre-drop virtualized task-thread UI; keep ThreadHeader visible (not under tracker) with an industry-standard max-width message column; hover floating actions on messages support Orch mention/embed/reference and answer-in-thread (not only the global agent); Orch mention is a composer badge with + that rotates to X (no `@` in the input row)—submit shows loading then an in-thread agent-avatar reply.
